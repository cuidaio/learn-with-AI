## CC，请执行以下任务：M2.8.1 — 关系提取 Prompt 紧凑化补丁

M2.8 已验收通过。本次任务是针对关系提取中 **Prefill 阶段输入过长** 的性能补丁，将实体列表从多行格式改为紧凑格式，预计节省 100-200 token，降低首字延迟 1-2s。

---

### 一、改动范围

仅修改 `backend/app/core/relation_extractor.py` 中的 Prompt 组装逻辑，不影响其他模块。

---

### 二、具体改动

#### 当前格式

```python
entity_list = "\n".join([f"- {e.name} ({e.entity_type})" for e in entities])
```

输出：
```
- 自我意识 (concept)
- 自我概念 (concept)
- 皮亚杰 (theorist)
- 形式运算 (concept)
- 埃里克森 (theorist)
- 认知失调 (concept)
```

#### 改为紧凑格式

```python
entity_list = ", ".join([f"{e.name}({e.entity_type})" for e in entities])
```

输出：
```
自我意识(concept), 自我概念(concept), 皮亚杰(theorist), 形式运算(concept), 埃里克森(theorist), 认知失调(concept)
```

---

### 三、改动后的完整 Prompt

```
[SYSTEM] 你是一位知识工程专家。以下是已从教材中提取的实体列表。请识别这些实体之间的关系。

【输出格式要求】
你必须输出纯 JSON 数组，不允许包含任何其他内容：
[
  {"source": "源实体名称", "target": "目标实体名称", "relation_type": "关系类型", "description": "关系描述（15字以内）"},
  ...
]

【关系类型】
- is_a: 上下位关系（如"自我意识" is_a "心理现象"）
- contains: 包含关系（如"自我意识" contains "自我概念"）
- causes: 因果关系（如"认知发展" causes "自我意识变化"）
- contrasts: 对比关系（如"皮亚杰" contrasts "维果茨基"）
- precedes: 时序关系（如"形式运算" precedes "后形式运算"）
- applies_to: 应用于（如"皮亚杰理论" applies_to "教育实践"）
- develops: 发展过程（如"自我评价" develops "自我概念"）

【实体列表】
{entity_list_compact}   ← 紧凑格式：实体名(类型), 实体名(类型), ...

【教材内容参考】
{aggregated_chunks}

【请开始提取关系】
```

---

### 四、改动量

| 文件 | 行数 | 改动类型 |
|------|------|----------|
| `relation_extractor.py` | ~5 行 | 修改 Prompt 拼接逻辑 |

---

### 五、验收标准

- [ ] 关系提取功能正常，输出结构与之前一致
- [ ] Prompt 中实体列表显示为紧凑格式
- [ ] 关系提取结果数量无明显下降（应保持与之前大致相同）
- [ ] API 响应时间略有改善（可忽略不计，仅 1-2s）

---

### 六、约束

1. **不修改 Prompt 内容**（仅修改实体列表格式）
2. **不修改实体提取逻辑**
3. **不修改关系提取的解析逻辑**
4. **不修改前端任何代码**

---

## CC，请开始 M2.8.1 的开发。完成后告诉我「M2.8.1 已完成，请验收」。